<Window x:Class="SqlServerTool.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="名前の変更" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="現在の名前" VerticalAlignment="Center" Margin="0,4"/>
        <TextBox   Grid.Row="0" Grid.Column="1" x:Name="OldNameBox"
                   IsReadOnly="True" Background="#F0F0F0" Margin="0,4"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="新しい名前" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox   Grid.Row="1" Grid.Column="1" x:Name="NewNameBox"
                   Margin="0,8"
                   KeyDown="NewNameBox_KeyDown"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                   x:Name="ErrorText"
                   Foreground="Red" TextWrapping="Wrap" Margin="0,4"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK"     Width="70" Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True"/>
            <Button Content="キャンセル" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
